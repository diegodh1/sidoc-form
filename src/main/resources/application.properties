server.port=${INTEGRAPPS_PORT:8080}
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.url=${DATABASE_URL:jdbc:sqlserver://10.10.17.11;databaseName=pro_logic_hercules}
spring.datasource.username=${DATABASE_USERNAME:hercules4}
spring.datasource.password=${DATABASE_PASSWORD:Colombia2021**}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql = true



## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServer2012Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#spring cache time limit
spring.cache.redis.time-to-live=120000
